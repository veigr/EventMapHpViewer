<Grid x:Class="EventMapHpViewer.ToolView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EventMapHpViewer"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignData /SampleData/ToolViewModelSampleData.xaml}"
             d:DesignHeight="300" d:DesignWidth="500">
    <Grid.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="4"/>
        </Style>
    </Grid.Resources>
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition/>
    </Grid.RowDefinitions>
    <TextBlock Text="공략중 맵 HP" Grid.Column="0" Grid.Row="0"/>
    <TextBlock Text="최저 필요 격파 회수" Grid.Column="0" Grid.Row="1"/>
    <TextBlock Text="{Binding NextEventMapHp}" Grid.Column="1" Grid.Row="0"/>
    <TextBlock Text="{Binding RemainingCount}" Grid.Column="1" Grid.Row="1"/>
    
    <TextBlock Style="{DynamicResource DefaultTextStyleKey}" Grid.Row="2" Grid.ColumnSpan="2">
        ※출격(해역선택)화면을 열때 갱신됩니다.<LineBreak/>
        ※맵 HP는 보스 기함의 HP를 소모시켜 감소시킵니다.<LineBreak/>
		※지원하지 않는 이벤트 해역이라도 남는 HP는 표시됩니다.<LineBreak/>	
        지원 이벤트：<LineBreak/>
        ・2015겨울<LineBreak/>
        ・2014가을<LineBreak/>
        ・2014여름<LineBreak/>
    </TextBlock>
</Grid>
